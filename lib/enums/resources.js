/**
 * Static Resources
 * @module
 */

/**
 * Enum with static resources that should always be available
 * @type {Object}
 */
const resources = {
    CONTACT: 'contact',
    USER: 'user',
    ROLE: 'role',
    PERMISSION: 'permission'
};

Object.freeze(resources);

module.exports = resources;
